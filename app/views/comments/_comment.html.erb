
<%= form_for([@post, @comment]) do |f| %>
    <div class='form-group'>
        <%= f.label 'New Comment' %>
        <%= f.text_area :body, class: 'form-control' %>
    </div>

    <%= f.submit "Add a comment", class: 'btn, btn-dark' %>
<% end %>
